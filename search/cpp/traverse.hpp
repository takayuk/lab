#include <queue>
#include <vector>

using namespace std;

int bf_search(const vector< vector<int> >& graph, const int& i) {
}


void traverse() {

  const int N = 10;
  
  vector< vector<int> > graph(N);
  vector<int> checktable(N, 0);
}


